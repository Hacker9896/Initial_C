// if and else statment programme
// TRUE  is everything except 0;
// False is 0;

#include <stdio.h>
int main(){
	int weather;
	 
	 printf("Enter the Number for weather =");
	 scanf("%d",&weather);
	 printf("\n");
	 
	 if(weather){
	 	printf("Weather is good today");
	 	
	 } else {
	 	printf("Weather is not good today");
	 }
}
