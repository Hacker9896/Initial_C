// Storing data in array
#include <stdio.h>
int main(){
	int abc[3];// here array can be written by any variable instead of abc
	 abc[0] = 1;
	 abc[1] = 3;
	 abc[2] = 5;
	printf("the number is %d " , abc[1]);
}
